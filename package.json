{
  "name": "tdoc",
  "private": true,
  "description": "Prompt-native interactive HTML documents — local server, Cloudflare Worker, browser overlay.",
  "scripts": {
    "test": "node test/run.js",
    "test:all": "node test/run.js --all"
  },
  "//devDependencies": "playwright is optional — only needed for the gated UI suites (ui/responsive/dimensions). Without it they skip loudly. Install with: npm i -D playwright && npx playwright install chromium",
  "optionalDependencies": {
    "playwright": "^1.60.0"
  }
}
